10 A% = 1
20 PRINT "padding line 20 to push RUN out of short-jump range"
25 PRINT "padding line 25 to push RUN out of short-jump range"
30 PRINT "padding line 30 to push RUN out of short-jump range"
35 PRINT "padding line 35 to push RUN out of short-jump range"
40 PRINT "padding line 40 to push RUN out of short-jump range"
45 PRINT "padding line 45 to push RUN out of short-jump range"
50 PRINT "padding line 50 to push RUN out of short-jump range"
55 PRINT "padding line 55 to push RUN out of short-jump range"
60 PRINT "padding line 60 to push RUN out of short-jump range"
65 PRINT "padding line 65 to push RUN out of short-jump range"
70 PRINT "padding line 70 to push RUN out of short-jump range"
75 PRINT "padding line 75 to push RUN out of short-jump range"
80 PRINT "padding line 80 to push RUN out of short-jump range"
85 PRINT "padding line 85 to push RUN out of short-jump range"
90 PRINT "padding line 90 to push RUN out of short-jump range"
95 PRINT "padding line 95 to push RUN out of short-jump range"
100 PRINT "padding line 100 to push RUN out of short-jump range"
105 PRINT "padding line 105 to push RUN out of short-jump range"
110 PRINT "padding line 110 to push RUN out of short-jump range"
115 PRINT "padding line 115 to push RUN out of short-jump range"
120 PRINT "padding line 120 to push RUN out of short-jump range"
125 PRINT "padding line 125 to push RUN out of short-jump range"
130 PRINT "padding line 130 to push RUN out of short-jump range"
135 PRINT "padding line 135 to push RUN out of short-jump range"
140 PRINT "padding line 140 to push RUN out of short-jump range"
145 PRINT "padding line 145 to push RUN out of short-jump range"
150 PRINT "padding line 150 to push RUN out of short-jump range"
155 PRINT "padding line 155 to push RUN out of short-jump range"
160 PRINT "padding line 160 to push RUN out of short-jump range"
165 PRINT "padding line 165 to push RUN out of short-jump range"
170 PRINT "padding line 170 to push RUN out of short-jump range"
175 PRINT "padding line 175 to push RUN out of short-jump range"
180 PRINT "padding line 180 to push RUN out of short-jump range"
185 PRINT "padding line 185 to push RUN out of short-jump range"
190 PRINT "padding line 190 to push RUN out of short-jump range"
195 PRINT "padding line 195 to push RUN out of short-jump range"
200 PRINT "padding line 200 to push RUN out of short-jump range"
900 IF A% = 2 THEN RUN
910 END
